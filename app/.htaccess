# Direct web access to application classes and templates (app/) is blocked.
# These files are only loaded via include in the entry points (admin.php / api.php),
# never directly. (The assets folder re-allows access with its own .htaccess.)
Require all denied
