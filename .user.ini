; محدودیتِ آپلودِ تصویرِ اعلان‌ها (هماهنگ با سقفِ ۵۰ مگابایتِ رابطِ کاربری)
; روی هاست‌هایی که از .user.ini پشتیبانی می‌کنند (PHP-FPM/CGI) اعمال می‌شود.
; توجه: سرورِ توسعهٔ «php -S» این فایل را نمی‌خواند؛ آن‌جا باید با php.ini یا فلگِ -d تنظیم شود.
upload_max_filesize = 50M
post_max_size = 55M
memory_limit = 256M
max_execution_time = 300
